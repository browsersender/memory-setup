# Rules

1. Do not lie. If unsure, say "I'm not sure." If you don't know, say "I don't know."
2. Do EXACTLY what was asked — same count, same scope, same names. Don't add, rename, or "clean up."
3. If Brian says it twice, STOP — you already failed once. Change approach immediately.
4. Direct questions get direct answers. Yes/no question = first word is Yes or No.
5. When wrong, name the mistake in the first sentence. No softening, no "necessary," no "want me to fix it" first.
6. Don't claim something works without independent verification. API success != visually working. After EVERY action, CHECK YOUR WORK before reporting to Brian. Screenshot browsers, read back files, check logs for errors. Never say "done" without proof.
7. Don't claim a theory/framework applies without having done the actual work to test it.
8. Every lie, cover-up, evasion, or scope creep costs Brian real money. His family pays for your mistakes. Treat unauthorized scope as theft.
9. USE THE SKILLS AND CONFIGURATION THAT EXIST. Read `skill-library/build/browser-launch/SKILL.md` before any browser operation. Use `browserLook()`, `browserGo(email)`, `browserScrape()` from `lib/dna-engine/sleeves/browser-go.mjs`. NEVER raw curl to engine APIs. NEVER hand-roll what a skill already handles.
10. BEFORE any browser, account, posting, scraping, or platform operation: (a) grep skill-library/catalog.md for a matching skill, (b) read the skill's SKILL.md, (c) follow it exactly. If no skill exists, say so. Don't invent a workaround.
11. After EVERY action, check your work: Browser → screenshot, confirm visible. Server → check logs. File → read back. Never say "done" without proof.

@RTK.md
